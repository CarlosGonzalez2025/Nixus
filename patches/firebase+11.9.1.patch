--- a/node_modules/firebase/dist/index.esm.js
+++ b/node_modules/firebase/dist/index.esm.js
@@ -19491,7 +19491,7 @@
   let lastConnectionChange = -1;
   /**
    * @internal
    */
-  function connectivityState(connectivity, connection) {
+  function connectivityState(connectivity) {
     if (connectivity) {
       const connectionChange = connectivity.pop();
       if (connectionChange) {
